package lib

import "testing"

func Test_threeWayHandshake(t *testing.T) {
	// deviceName := "eth0"
	// 启动抓包程序 threeWayHandshake(deviceName)
	// 启动 Server("0.0.0.0:8080")
	// 启动 Client("127.0.0.1:8080") 即可看到三次握手过程
}